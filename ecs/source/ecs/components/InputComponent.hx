package ecs.components;

import ecs.IComponent;

/**
 * A marker component to identify an entity as being controllable by user input.
 */
class InputComponent implements IComponent
{
	public function new() {}
}
